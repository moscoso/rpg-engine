package model.ability;

import model.entity.Avatar;

public class FireBlast extends Ability {
    
    @Override
    public void execute() {
        // Avatar avatar = Avatar.getInstance();
        // int manaCostToShoot = 5;
        // int current = avatar.myStats.getCurrentMana();
        // if (current >= manaCostToShoot) {
        //     Point location = avatar.getLocation();

        //     CombatCoordinator.spawnProjectile(ProjectileFactory.newFireBall(location.x, location.y, ));
        //     CombatCoordinator.spawnProjectile(ProjectileFactory.newFireBall(avatar));
        //     avatar.myStats.setCurrentMana(current - manaCostToShoot);
        // }
        // Avatar.canShoot = false;
        // GameTimer ability1Cooldown = new GameTimer(15);
        // ability1Cooldown.setGameTimerListener(new GameTimerListener() {

        //     @Override
        //     public void trigger() {
        //         canShoot = true;
        //     }

        // });
        // ability1Cooldown.start();
    }
}
